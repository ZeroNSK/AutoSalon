# 🐧 Установка AutoSalon на Linux

## 🚀 Быстрый старт (3 команды)

### 1. Установить Docker

**Ubuntu/Debian:**
```bash
sudo apt update && sudo apt install -y docker.io docker-compose git
```

**CentOS/RHEL:**
```bash
sudo yum install -y docker docker-compose git
sudo systemctl start docker && sudo systemctl enable docker
```

**Arch Linux:**
```bash
sudo pacman -S docker docker-compose git
sudo systemctl start docker && sudo systemctl enable docker
```

### 2. Клонировать и запустить проект

```bash
# Клонировать репозиторий (замените на ваш URL)
git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
cd YOUR_REPO_NAME

# Запустить приложение
docker-compose up -d --build
```

### 3. Открыть в браузере

```bash
# Подождать 1-2 минуты, затем открыть:
firefox http://localhost:6080
# или
google-chrome http://localhost:6080
```

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка системы

```bash
# Обновить систему
sudo apt update && sudo apt upgrade -y

# Установить необходимые пакеты
sudo apt install -y \
    docker.io \
    docker-compose \
    git \
    curl \
    wget \
    firefox

# Запустить Docker
sudo systemctl start docker
sudo systemctl enable docker

# Добавить пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER

# Перелогиниться или выполнить:
newgrp docker
```

### Шаг 2: Получить проект

```bash
# Создать папку для проектов
mkdir -p ~/projects
cd ~/projects

# Клонировать репозиторий
git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
cd YOUR_REPO_NAME

# Проверить содержимое
ls -la
```

### Шаг 3: Запустить приложение

```bash
# Собрать и запустить контейнеры
docker-compose up -d --build

# Проверить статус
docker-compose ps
```

**Ожидаемый вывод:**
```
       Name                     Command               State                    Ports                  
----------------------------------------------------------------------------------------------------
autosalon_app        /usr/bin/supervisord             Up      0.0.0.0:5900->5900/tcp, 0.0.0.0:6080->80/tcp
autosalon_db         docker-entrypoint.sh postgres    Up      0.0.0.0:5432->5432/tcp
```

### Шаг 4: Проверить работу

```bash
# Посмотреть логи приложения
docker logs autosalon_app

# Посмотреть логи базы данных
docker logs autosalon_db

# Проверить, что порты открыты
netstat -tulpn | grep -E "(6080|5900|5432)"
```

### Шаг 5: Открыть GUI

```bash
# Установить браузер (если нет)
sudo apt install -y firefox

# Открыть приложение
firefox http://localhost:6080 &

# Или через командную строку
curl -I http://localhost:6080
```

---

## 🔧 Устранение проблем

### Проблема: Docker не запускается

```bash
# Проверить статус Docker
sudo systemctl status docker

# Запустить Docker
sudo systemctl start docker

# Проверить права пользователя
groups $USER
# Должна быть группа "docker"
```

### Проблема: Порты заняты

```bash
# Найти процессы на портах
sudo netstat -tulpn | grep -E "(6080|5900|5432)"

# Остановить конфликтующие процессы или изменить порты в docker-compose.yml
```

### Проблема: GUI не отображается

```bash
# Подождать 2-3 минуты после запуска
sleep 120

# Проверить логи VNC
docker logs autosalon_app | grep -i vnc

# Перезапустить контейнеры
docker-compose restart
```

### Проблема: Ошибки сборки

```bash
# Очистить Docker кэш
docker system prune -f

# Пересобрать с нуля
docker-compose down -v
docker-compose up -d --build --no-cache
```

---

## 🌐 Удаленный доступ

Если устанавливаете на сервер и хотите доступ с другого компьютера:

### 1. Открыть порты в файрволе

```bash
# Ubuntu/Debian
sudo ufw allow 6080
sudo ufw allow 5900

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=6080/tcp
sudo firewall-cmd --permanent --add-port=5900/tcp
sudo firewall-cmd --reload
```

### 2. Узнать IP адрес сервера

```bash
# Внутренний IP
ip addr show | grep inet

# Внешний IP (если есть)
curl ifconfig.me
```

### 3. Подключиться с другого компьютера

- **Браузер:** `http://SERVER_IP:6080`
- **VNC клиент:** `SERVER_IP:5900`

---

## ✅ Проверка функциональности

После запуска проверьте все функции:

1. **Просмотр таблицы** - должны быть видны столбцы
2. **Добавление записи** - заполните форму и добавьте автомобиль
3. **Поиск по цене** - введите максимальную цену
4. **Экспорт в файл** - сохраните данные в файл
5. **Чтение файла** - откройте сохраненный файл
6. **Обновление цены** - измените цену для марки
7. **Удаление по производителю** - удалите автомобили
8. **База данных** - данные должны сохраняться после перезапуска

---

## 🎯 Готово к демонстрации!

**URL для демонстрации:** `http://localhost:6080` (или `http://SERVER_IP:6080`)

**Команды для управления:**
```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f
```

---

## 📱 Системные требования

### Минимальные требования

- **ОС:** Ubuntu 18.04+, CentOS 7+, Debian 9+, или любой Linux с Docker
- **RAM:** 4 ГБ
- **Диск:** 2 ГБ свободного места
- **CPU:** 2 ядра
- **Сеть:** Доступ к интернету для загрузки Docker образов

### Рекомендуемые требования

- **RAM:** 8 ГБ
- **Диск:** 5 ГБ свободного места
- **CPU:** 4 ядра
- **Браузер:** Firefox, Chrome, или любой современный браузер

---

## 🔄 Обновление проекта

```bash
# Получить последние изменения
git pull origin main

# Пересобрать и запустить
docker-compose down
docker-compose up -d --build
```

---

## 🗑️ Полное удаление

```bash
# Остановить и удалить контейнеры
docker-compose down -v

# Удалить образы
docker rmi $(docker images | grep autosalon | awk '{print $3}')

# Удалить проект
cd .. && rm -rf YOUR_REPO_NAME
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker logs autosalon_app`
2. Убедитесь, что Docker запущен: `sudo systemctl status docker`
3. Проверьте порты: `netstat -tulpn | grep 6080`
4. Перезапустите: `docker-compose restart`

**Ваше приложение готово к защите курсовой работы!** 🎉