# 🖥️ Как открыть GUI приложения

## ✅ Приложение уже запущено!

Я запустил приложение с встроенным VNC сервером Qt.

---

## 🚀 Способ 1: Через VNC клиент (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Установите VNC клиент

**На macOS:**
```bash
brew install --cask vnc-viewer
```

Или скачайте RealVNC Viewer: https://www.realvnc.com/download/viewer/

**Альтернативы:**
- TigerVNC Viewer
- TightVNC
- Встроенный Screen Sharing (Finder → Go → Connect to Server)

### Шаг 2: Подключитесь к VNC

1. Откройте VNC клиент
2. Введите адрес: `localhost:5900`
3. Нажмите Connect
4. Пароль: оставьте пустым или попробуйте `password`

### Шаг 3: Работайте с приложением!

Вы увидите окно Qt приложения "Автосалон - Курсовая работа" с:
- Таблицей данных
- 7 кнопками управления

---

## 🍎 Способ 2: Встроенный Screen Sharing (macOS)

1. Откройте Finder
2. Нажмите `Cmd + K` (или Go → Connect to Server)
3. Введите: `vnc://localhost:5900`
4. Нажмите Connect
5. Если попросит пароль, оставьте пустым

---

## 🔧 Управление приложением

### Проверить, что приложение запущено:
```bash
docker exec autosalon_app ps aux | grep AutoSalon
```

### Остановить приложение:
```bash
docker exec autosalon_app pkill -f AutoSalon
```

### Перезапустить приложение:
```bash
./start_gui.sh
```

### Посмотреть логи приложения:
```bash
docker logs autosalon_app
```

---

## 🎮 Что можно делать в GUI

После подключения вы сможете:

1. **Просмотр данных** - таблица показывает все автомобили
2. **Добавить автомобиль** - кнопка "Добавить"
3. **Фильтр по цене** - кнопка "Фильтр по цене"
4. **Обновить цену** - кнопка "Изменить цену"
5. **Удалить по производителю** - кнопка "Удалить по производителю"
6. **Экспорт в файл** - кнопка "Сформировать файл"
7. **Показать файл** - кнопка "Показать файл"
8. **Обновить таблицу** - кнопка "Обновить таблицу"

---

## 🐛 Решение проблем

### Проблема: VNC клиент не подключается

**Решение 1:** Проверьте, что приложение запущено
```bash
docker exec autosalon_app ps aux | grep AutoSalon
```

Если не запущено:
```bash
./start_gui.sh
```

**Решение 2:** Проверьте, что порт 5900 не занят
```bash
lsof -i :5900
```

Если занят другим процессом, остановите его или измените порт.

**Решение 3:** Перезапустите контейнер
```bash
docker-compose restart app
./start_gui.sh
```

---

### Проблема: Приложение не запускается

Проверьте логи:
```bash
docker exec autosalon_app bash -c "cd /app/build && QT_QPA_PLATFORM=vnc ./AutoSalon 2>&1"
```

---

### Проблема: Не могу установить VNC клиент

**Альтернатива:** Используйте встроенный Screen Sharing на macOS:
1. Откройте Safari или любой браузер
2. Нажмите `Cmd + K` в Finder
3. Введите `vnc://localhost:5900`

Или используйте командную строку для тестирования:
```bash
# Войти в контейнер
docker exec -it autosalon_app bash

# Запустить приложение в offscreen режиме (без GUI)
cd /app/build
QT_QPA_PLATFORM=offscreen ./AutoSalon
```

---

## 📸 Скриншоты для защиты

Если нужны скриншоты для отчета:

1. Подключитесь через VNC
2. Используйте встроенные инструменты скриншотов macOS:
   - `Cmd + Shift + 3` - весь экран
   - `Cmd + Shift + 4` - выделенная область
   - `Cmd + Shift + 5` - расширенные опции

---

## 🎯 Быстрый тест GUI

После подключения через VNC:

1. **Добавьте автомобиль:**
   - Нажмите "Добавить"
   - Заполните: Camry, Toyota, 2020-01-15, 50000, 1500000
   - Нажмите OK
   - ✅ Запись появится в таблице

2. **Фильтр по цене:**
   - Нажмите "Фильтр по цене"
   - Введите: 2000000
   - ✅ Увидите только автомобили дешевле 2 млн

3. **Обновите цену:**
   - Нажмите "Изменить цену"
   - Марка: Camry
   - Новая цена: 1600000
   - ✅ Цена обновится в таблице

4. **Экспорт:**
   - Нажмите "Сформировать файл"
   - ✅ Увидите сообщение об успехе

5. **Показать файл:**
   - Нажмите "Показать файл"
   - ✅ Увидите содержимое экспортированного файла

---

## 💡 Полезные команды

```bash
# Запустить GUI
./start_gui.sh

# Проверить статус
docker exec autosalon_app ps aux | grep AutoSalon

# Остановить GUI
docker exec autosalon_app pkill -f AutoSalon

# Перезапустить контейнеры
docker-compose restart

# Посмотреть все процессы в контейнере
docker exec autosalon_app ps aux
```

---

## ✅ Готово!

Теперь вы можете:
1. ✅ Подключиться через VNC клиент к `localhost:5900`
2. ✅ Работать с GUI приложения
3. ✅ Тестировать все 8 функций
4. ✅ Делать скриншоты для отчета

**Приложение работает и готово к демонстрации!** 🎉
