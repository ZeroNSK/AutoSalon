# 🚀 Как протестировать приложение

## Быстрый старт

### 1️⃣ Автоматический тест (РЕКОМЕНДУЕТСЯ)

Просто запустите:
```bash
./test_manual.sh
```

Этот скрипт автоматически проверит:
- ✅ Подключение к БД
- ✅ Структуру таблицы и индексы
- ✅ Добавление записей
- ✅ Фильтрацию по цене
- ✅ Обновление цены
- ✅ Удаление по производителю
- ✅ Сборку приложения

---

## Ручное тестирование

### 2️⃣ Проверить текущие данные

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c 'SELECT * FROM cars;'
```

### 3️⃣ Добавить новую запись

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c \
  "INSERT INTO cars (brand, manufacturer, release_date, mileage, price) \
   VALUES ('Camry', 'Toyota', '2020-01-15', 50000, 1500000);"
```

### 4️⃣ Фильтрация по цене

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c \
  'SELECT * FROM cars WHERE price <= 2000000;'
```

### 5️⃣ Обновить цену

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c \
  "UPDATE cars SET price = 1600000 WHERE brand = 'Camry';"
```

### 6️⃣ Удалить по производителю

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c \
  "DELETE FROM cars WHERE manufacturer = 'Toyota';"
```

### 7️⃣ Проверить структуру таблицы

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c '\d cars'
```

### 8️⃣ Проверить индексы

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c '\di'
```

---

## Тест персистентности данных

```bash
# 1. Добавить данные (см. выше)

# 2. Остановить контейнеры
docker-compose down

# 3. Запустить снова
docker-compose up -d

# 4. Проверить, что данные сохранились
docker exec -it autosalon_db psql -U user -d autosalon -c 'SELECT * FROM cars;'
```

---

## Запуск приложения вручную

Если хотите запустить Qt приложение:

```bash
# Войти в контейнер
docker exec -it autosalon_app bash

# Запустить приложение
cd /app/build
./AutoSalon
```

**Примечание:** Для полноценного GUI нужен VNC клиент или настроенный X11 forwarding.

---

## Полезные команды

### Посмотреть логи

```bash
# Логи приложения
docker logs autosalon_app

# Логи базы данных
docker logs autosalon_db
```

### Перезапустить систему

```bash
docker-compose restart
```

### Остановить систему

```bash
docker-compose down
```

### Полностью удалить (включая данные)

```bash
docker-compose down -v
```

---

## 📊 Результаты тестирования

Подробные результаты всех тестов смотрите в файле:
**`TESTING_RESULTS.md`**

---

## ✅ Статус

**Все 8 функций работают корректно!**

1. ✅ Создание таблицы
2. ✅ Просмотр записей
3. ✅ Поиск по цене
4. ✅ Экспорт в файл
5. ✅ Чтение файла
6. ✅ Добавление записи
7. ✅ Удаление по производителю
8. ✅ Обновление цены

**Готовность к защите: 100%** 🎉
