# 📝 ШПАРГАЛКА - Быстрый доступ

## 🚀 Запуск

```bash
# Запустить всё
docker-compose up -d

# Запустить GUI
./start_gui.sh
```

## 🖥️ Открыть GUI

**Вариант 1: VNC клиент**
- Адрес: `localhost:5900`
- Пароль: (пустой)
- **Важно:** Используйте масштабирование "Fit to window" для полного отображения

**Вариант 2: macOS Screen Sharing**
1. Finder → `Cmd + K`
2. Введите: `vnc://localhost:5900`
3. View → Scale to Fit Available Space

**Если не видны все колонки/кнопки:**
- Отключитесь и подключитесь заново
- Или используйте полноэкранный режим (F11)
- Или перезапустите: `./start_gui.sh`

## 🧪 Автоматический тест

```bash
./test_manual.sh
```

## 📊 Проверить данные

```bash
# Показать все записи
docker exec -it autosalon_db psql -U user -d autosalon -c 'SELECT * FROM cars;'

# Структура таблицы
docker exec -it autosalon_db psql -U user -d autosalon -c '\d cars'

# Индексы
docker exec -it autosalon_db psql -U user -d autosalon -c '\di'
```

## ➕ Добавить запись

```bash
# Примечание: brand = производитель, manufacturer = модель
docker exec -it autosalon_db psql -U user -d autosalon -c \
  "INSERT INTO cars (brand, manufacturer, release_date, mileage, price) \
   VALUES ('Toyota', 'Camry', '2020-01-15', 50000, 1500000);"
```

**В GUI формат даты:** ДД.ММ.ГГГГ (15.01.2020)  
**Ограничение:** год до 2025 включительно

## 🔍 Фильтр по цене

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c \
  'SELECT * FROM cars WHERE price <= 2000000;'
```

## 💰 Обновить цену

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c \
  "UPDATE cars SET price = 1600000 WHERE brand = 'Camry';"
```

## 🗑️ Удалить по производителю

```bash
docker exec -it autosalon_db psql -U user -d autosalon -c \
  "DELETE FROM cars WHERE manufacturer = 'Toyota';"
```

## 🔄 Управление

```bash
# Перезапустить
docker-compose restart

# Остановить
docker-compose down

# Остановить GUI
docker exec autosalon_app pkill -f AutoSalon

# Проверить статус GUI
docker exec autosalon_app ps aux | grep AutoSalon

# Логи
docker logs autosalon_app
docker logs autosalon_db
```

## 📁 Документация

- `КАК_ОТКРЫТЬ_GUI.md` - подробная инструкция по GUI
- `КАК_ТЕСТИРОВАТЬ.md` - инструкция по тестированию
- `TESTING_RESULTS.md` - результаты всех тестов
- `README.md` - общая документация

## ✅ Чек-лист для защиты

- [ ] Контейнеры запущены: `docker-compose ps`
- [ ] БД работает: `docker exec -it autosalon_db psql -U user -d autosalon -c 'SELECT * FROM cars;'`
- [ ] GUI запущен: `./start_gui.sh`
- [ ] VNC подключен: `localhost:5900`
- [ ] Все 8 функций работают
- [ ] Скриншоты сделаны

## 🎯 Сценарий демонстрации (5 минут)

1. **Запуск** (30 сек)
   ```bash
   docker-compose up -d
   ./start_gui.sh
   ```

2. **Подключение VNC** (30 сек)
   - Открыть VNC клиент
   - Подключиться к `localhost:5900`

3. **Демонстрация функций** (3 мин)
   - Показать таблицу с данными
   - Добавить новую запись
   - Фильтр по цене
   - Обновить цену
   - Удалить по производителю
   - Экспорт в файл
   - Показать файл

4. **Проверка БД** (1 мин)
   ```bash
   docker exec -it autosalon_db psql -U user -d autosalon -c '\d cars'
   docker exec -it autosalon_db psql -U user -d autosalon -c 'SELECT * FROM cars;'
   ```

## 🆘 Если что-то не работает

```bash
# Полный перезапуск
docker-compose down
docker-compose up -d
./start_gui.sh

# Проверить логи
docker logs autosalon_app --tail 50
docker logs autosalon_db --tail 50

# Войти в контейнер
docker exec -it autosalon_app bash
```

## 📞 Быстрая помощь

**GUI не открывается?**
→ Проверьте: `docker exec autosalon_app ps aux | grep AutoSalon`
→ Перезапустите: `./start_gui.sh`

**БД не отвечает?**
→ Проверьте: `docker-compose ps`
→ Перезапустите: `docker-compose restart db`

**Нет данных?**
→ Добавьте тестовые: `./test_manual.sh`

---

**Всё готово! Удачи на защите! 🎉**
